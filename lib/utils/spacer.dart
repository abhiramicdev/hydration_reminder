import 'package:flutter/material.dart';

class spacer12 extends StatelessWidget {
  const spacer12({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 12);
  }
}
